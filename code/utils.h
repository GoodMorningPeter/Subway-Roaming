#pragma once
#ifndef UTILS_H
#define UTILS_H

#endif // UTILS_H

#pragma once
#include <deque>
#include <vector>
#include <algorithm>
#include <cmath>
#include <iostream>
#include <iomanip>
#include <set>
#include <random>
#include <ctime>

//class utils{
//public:
//    std::random_device seed;
//    std::ranlux48 engine(this->seed());
//    std::uniform_int_distribution<> distrib(0, 1000000);
//    double myrandom() { return (distrib(engine)%1000000)/1000000.0; }
//};

//std::random_device Graph::seed;
//std::ranlux48 Graph::engine(seed());
//std::uniform_int_distribution<> Graph::distrib(0, 1000000);

